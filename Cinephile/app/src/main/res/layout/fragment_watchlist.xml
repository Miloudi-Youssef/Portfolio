<?xml version="1.0" encoding="utf-8"?>
<!-- watchlist list screen — CoordinatorLayout so FAB floats over the RecyclerView -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    android:paddingTop="20dp">

    <!-- watchlist RecyclerView — swipe-delete enabled by ItemTouchHelper in the fragment -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_watchlists"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false" />

    <!-- empty state — shown when no watchlists exist -->
    <TextView
        android:id="@+id/tv_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/empty_watchlists"
        android:textColor="@color/colorSubtext"
        android:textSize="15sp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:padding="32dp"
        android:visibility="gone" />

    <!-- FAB — create new watchlist, bottom-end anchored -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@drawable/ic_add"
        app:backgroundTint="@color/colorPrimary"
        android:contentDescription="@string/new_watchlist" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
