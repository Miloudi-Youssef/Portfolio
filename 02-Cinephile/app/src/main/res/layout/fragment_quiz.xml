<?xml version="1.0" encoding="utf-8"?>
<!-- quiz list screen — create button, empty state, quiz card list -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground"
    android:paddingTop="20dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingBottom="12dp">

    <!-- create quiz button — triggers watchlist → difficulty picker dialogs -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_new_quiz"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/new_quiz"
        android:textColor="@color/white"
        app:backgroundTint="@color/colorPrimary"
        android:layout_marginTop="4dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="12dp" />

    <!-- empty state — shown when no quizzes have been created -->
    <TextView
        android:id="@+id/tv_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/empty_quizzes"
        android:textColor="@color/colorSubtext"
        android:textSize="15sp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:padding="32dp"
        android:visibility="gone" />

    <!-- quiz cards RecyclerView — QuizAdapter with long-press delete -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_quizzes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false" />

</LinearLayout>
